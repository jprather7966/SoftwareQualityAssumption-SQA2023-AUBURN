name: Fuzz Script

on:
  push:
    branches: [ master ]

jobs:
  fuzzer:
    name: Fuzzer
    runs-on: ubuntu-latest
    steps:
      - uses: jprather7966/SoftwareQualityAssumption-SQA2023-AUBURN/fuzz.py@master
